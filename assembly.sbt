import AssemblyKeys._ // put this at the top of the file

assemblySettings

//mainClass in assembly := Some("ru.wordmetrix.enrichers.WholeLotOfPictures")
mainClass in assembly := Some("ru.wordmetrix.enrichers.Vocabulary")
